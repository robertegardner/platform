# Tier 2 — Distribution (rack): Icecast + NPMplus routing. STUB — no resources
# yet. Built in a later phase (after the dx-R2 transport proof passes). When
# built, copy the container + null_resource provision pattern from
# homelab-monitor's module.monitoring.
