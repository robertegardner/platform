# Tier 3 — Radio compute (rack LXC): FM mux/stereo + AM + Meteor/wxsat (SatDump).
# STUB — no resources yet. Built in a later phase. When built, copy the container
# + null_resource provision pattern from homelab-monitor's module.monitoring.
